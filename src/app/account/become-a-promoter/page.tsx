import { BecomePromoter } from "@/components/account/BecomePromoter";

/** Private, like the rest of /account — the layout's robots metadata covers
 *  this subtree, so nothing here is indexed. */
export default function BecomePromoterPage() {
  return <BecomePromoter />;
}
